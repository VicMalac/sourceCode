//indice = 0.0588 * L - 0.296 * S - 15.8 L indica o numero de letras para cada 100 palavras e S indica o numero de sentenças para cada 10 palavras.
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main(void){
    //Pede a frase
    char phrase[100];
    printf("Text: ");
    scanf(" %[^\n]", phrase);
    printf("%s", phrase);
    //for (int i = 0, n = strlen(phrase); i < n; i++){
    //    printf("%c", phrase[1]);
    //}

    //Identificar quantas palvras tem

    //Identificar quantas letras tem

    //Identificar quantas sentenças tem

    //Imprimir a Grade de 1 a 16+
}